{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
"scripts": {
    "start": "node index.js", 
    "frontend-build": "cd ../client && npm config set legacy-peer-deps=true && npm install && npm run build",
    "backend-build": "npm config set legacy-peer-deps=true && npm install",
    "build": "npm run frontend-build && npm run backend-build",
    "dev": "node index.js"
},
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@clerk/express": "^1.3.18",
    "cors": "^2.8.5",
    "express": "^5.0.1",
    "imagekit": "^5.2.0",
    "mongoose": "^8.8.2",
    "svix": "^1.41.0"
  }
}
